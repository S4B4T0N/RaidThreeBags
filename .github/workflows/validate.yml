name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  static-validation:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate release and Warperia layout
        shell: pwsh
        run: ./scripts/Validate-Release.ps1
