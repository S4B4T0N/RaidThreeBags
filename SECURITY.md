# Security policy

RaidThreeBags does not require network access or credentials. Never attach account data, SavedVariables, character names or server-identifying screenshots to a public report without redaction.

Reports involving item loss, unintended bag replacement, protected-action errors or another addon’s UI being broken should include exact reproduction steps and a redacted Lua stack trace. Use a private GitHub security advisory for a vulnerability after the repository exists.
